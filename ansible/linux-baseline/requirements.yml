---
# Collections the baseline role uses. Install once on the control node:
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.general      # ufw, timezone modules
  - name: ansible.posix          # sysctl module
