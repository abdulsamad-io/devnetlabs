[defaults]
inventory = inventory.yml
roles_path = roles
retry_files_enabled = False
stdout_callback = yaml
host_key_checking = False
# Baseline runs over SSH with sudo (unlike the API-driven technitium project).

[privilege_escalation]
become = True
become_method = sudo
